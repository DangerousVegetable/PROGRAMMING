#include<iostream>
#include <vector>
using namespace std;

vector<int>a;
vector<int>b;
vector<int>l;
vector<int>r;

bool go(int it, int n)
{
    vector<int> aa;
    vector<int> bb;
    for(int i = 0; i < )
}

int main()
{
    int n;
    cin >> n;
    
    a.resize(n);
    b.resize(n);
    l.resize(n);
    r.resize(n);

    for(int i = 0; i < n; i++)
    {
        cin >> a[i];
    }
    for(int i = 0; i < n; i++)
    {
        cin >> b[i];
    }

    for(int i = 0; i < n; i++)
    {
        cin >> l[n] >> r[n];
    }


}